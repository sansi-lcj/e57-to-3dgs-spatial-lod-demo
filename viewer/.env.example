# Leave unset to load the bundled viewer/public/lod/manifest.json.
# Set an absolute URL when serving the LoD package from a CDN with CORS enabled.
# VITE_LOD_MANIFEST_URL=https://cdn.example.com/p4-lod/manifest.json
